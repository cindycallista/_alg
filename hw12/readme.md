# Cross Entropy Minimization with Hill Climbing

This project demonstrates that the **cross-entropy** between two probability distributions `p` and `q` is minimized when `q = p`. 

It uses a **hill climbing algorithm** to iteratively adjust `q` to minimize `cross_entropy(p, q)`.
